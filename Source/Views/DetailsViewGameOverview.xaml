
    <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style TargetType="{x:Type DetailsViewGameOverview}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DetailsViewGameOverview}">
                    <Grid Margin="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.RowSpan="2">
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Grid.Row="0"
                                      Style="{DynamicResource DetailsScrollViewer}"
                                      d:DesignWidth="1280"
                                      d:DesignHeight="1280"
                                      d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameDetailsIntance}"
                                      x:Name="PART_ScrollViewHost">

                                <Grid MaxWidth="{DynamicResource DetailsViewGameOverviewMaxWidth}" MinWidth="300" Margin="24,0,24,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="300"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <!--<RowDefinition Height="{Settings CalculatedGameDetailsIndentation}"/>-->
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <!--Right Side-->
                                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0">
                                        
                                        <!--Extra Logo-->
                                        <Grid Height="260" Margin="0,0,0,40">
                                            <Border x:Name="GameIcon" CornerRadius="{StaticResource ControlCornerRadius}">
                                                <Image Name="PART_ImageIcon" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                       RenderOptions.BitmapScalingMode="Fant" MaxHeight="120" MaxWidth="120"/>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=EnableLogos, FallbackValue=False}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsLogoAvailable, FallbackValue=False}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}, FallbackValue=False}" Value="false" >
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                            <Border x:Name="ExtraMetadataLoader_LogoLoaderControlGrid" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                                    Tag="{DynamicResource DetailsViewAllowUseOfLogos}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=EnableLogos, FallbackValue=False}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsLogoAvailable, FallbackValue=False}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}, FallbackValue=False}" Value="false" >
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <ContentControl x:Name="ExtraMetadataLoader_LogoLoaderControl"/>
                                            </Border>
                                        </Grid>
                                        <!--Extra Logo Ends-->
                                            
                                        <!--Aciton Buttons-->
                                        <StackPanel Margin="0,0,0,32">

                                                <Grid>
                                                    <Button Name="PART_ButtonPlayAction" Style="{DynamicResource DetailPlayButton}"  />

                                                    <Button Name="PART_ButtonContextAction" Style="{DynamicResource DetailPlayButton}" />

                                                    <Button Name="PART_ButtonMoreActions" HorizontalAlignment="Right"
                                                            Content="&#xe228;" Style="{DynamicResource DetailPlayButtonRightPart}" />
                                                </Grid>

                                                <!--<Button x:Name="PART_ButtonEditGame" Margin="4,0,4,0" Focusable="False" Content="&#xe115;" 
                                                            Style="{StaticResource DetailPlayButton}"/>-->
                                                <Button x:Name="PART_ButtonEditGame" Margin="0,8,0,0" Focusable="False"
                                                        Content="{DynamicResource LOCEditGame}"  Height="44" FontSize="{StaticResource FontSizeLarge}"
                                                        Style="{StaticResource SecondaryButton}"/>

                                                <!--<Button Name="PART_ButtonMoreActions" Margin="4,0,4,0" Content="&#xe700;"
                                                            Style="{DynamicResource DetailPlayButton}" />-->

                                                <ToggleButton Name="GameFavoriteToggleButton" Margin="0,8,0,0" Focusable="False"
                                                              Style="{DynamicResource FavoriteRoundedToggleButton}"
                                                              Visibility="{PluginStatus Plugin=felixkmh_Extras_Plugin}"
                                                              IsChecked="{PluginSettings Plugin=ThemeExtras, Path=Game.Favorite, Mode=TwoWay, FallbackValue={StaticResource False}}"/>
                                            </StackPanel>
                                        <!--Aciton Buttons Ends-->

                                        <!--HowLongToBeat-->
                                        <StackPanel Margin="0,0,0,32" Visibility="{PluginSettings Plugin=HowLongToBeat, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="{DynamicResource LOCHowLongToBeat}" Style="{DynamicResource BaseTextBlockStyle}" FontWeight="Bold" FontSize="14"/>

                                                <Border CornerRadius="{DynamicResource ControlCornerRadius}" 
                                                        Style="{DynamicResource DetailViewCardBorder}" Margin="0,12,0,0">

                                                    <StackPanel Margin="0,0,0,0">
                                                        <StackPanel.Resources>
                                                            <Style TargetType="Label" x:Key="HltbGridLabelStyle">
                                                                <Setter Property="FontSize" Value="{DynamicResource FontSizeSmaller}"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource IdleBrush}"/>
                                                                <Setter Property="HorizontalAlignment" Value="Center" />
                                                            </Style>
                                                            <Style TargetType="Border" x:Key="HltbBorderStyle">
                                                                <Setter Property="Background" Value="{DynamicResource IdleBrush}"/>
                                                                <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}"/>
                                                            </Style>
                                                            <Style TargetType="TextBlock" x:Key="HltbGridTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}">
                                                                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}"/>
                                                                <Setter Property="Margin" Value="8,8,8,8" />
                                                                <Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Text}" Value="--">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Resources>

                                                        <Grid Margin="16,12,16,16">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>



                                                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                                                <Label Content="{DynamicResource LOCHowLongToBeatMainStory}"
                                                                       Style="{DynamicResource HltbGridLabelStyle}"
                                                                       Visibility="{Binding ElementName=MainStoryFormat, Path=Visibility}" />
                                                                <Border Style="{DynamicResource HltbBorderStyle}">
                                                                    <TextBlock Name="MainStoryFormat" Style="{DynamicResource HltbGridTextStyle}"
                                                                               Text="{PluginSettings Plugin=HowLongToBeat, Path=MainStoryFormat, FallbackValue='--'}"/>
                                                                </Border>
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="1" Margin="0,0,12,0">

                                                                <Label Content="{DynamicResource LOCHowLongToBeatMainExtra}"
                                                                       Style="{DynamicResource HltbGridLabelStyle}"
                                                                       Visibility="{Binding ElementName=MainExtraFormat, Path=Visibility}" />
                                                                <Border Style="{DynamicResource HltbBorderStyle}">
                                                                    <TextBlock Name="MainExtraFormat"
                                                                               Style="{DynamicResource HltbGridTextStyle}"
                                                                               Text="{PluginSettings Plugin=HowLongToBeat, Path=MainExtraFormat, FallbackValue='--'}"/>
                                                                </Border>
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="2" Margin="0,0,0,0">

                                                                <Label Content="{DynamicResource LOCHowLongToBeatCompletionist}"
                                                                       Style="{DynamicResource HltbGridLabelStyle}"
                                                                       Visibility="{Binding ElementName=CompletionistFormat, Path=Visibility}" />
                                                                <Border Style="{DynamicResource HltbBorderStyle}">
                                                                    <TextBlock Name="CompletionistFormat"
                                                                               Style="{DynamicResource HltbGridTextStyle}"
                                                                               Text="{PluginSettings Plugin=HowLongToBeat, Path=CompletionistFormat, FallbackValue='--'}"/>
                                                                </Border>
                                                            </StackPanel>

                                                        </Grid>

                                                        <Border Background="{DynamicResource GlobalBackgroundBrush}" CornerRadius="0,0,16,16">
                                                            <!--<DockPanel Margin="16,16,16,16">
                                                                <TextBlock Text="{DynamicResource LOCTimePlayed}" Foreground="{DynamicResource IdleBrush}" DockPanel.Dock="Left"/>
                                                                <TextBlock Name="PART_TextPlayTime" DockPanel.Dock="Right" TextAlignment="Right" FontWeight="Bold"/>
                                                            </DockPanel>-->
                                                        </Border>

                                                    </StackPanel>

                                                </Border>
                                            </StackPanel>
                                        <!--HowLongToBeat Ends-->                                    

                                        <!--Details Panel-->
                                        <StackPanel Margin="0,0,0,32">
                                            <StackPanel.Resources>
                                                <Style TargetType="ItemsControl">
                                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                                    <Setter Property="Margin" Value="0,8,0,0" />
                                                </Style>
                                                <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
                                                    <Setter Property="Margin" Value="0,12,12,0" />
                                                    <Setter Property="VerticalAlignment" Value="Top" />
                                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource PromptBrush}" />
                                                    <Setter Property="MaxWidth" Value="140"/>
                                                </Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}">
                                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                                    <Setter Property="Margin" Value="0,4,0,0" />
                                                </Style>
                                                        <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" >
                                                            <Setter Property="Margin" Value="0,8,0,0" />
                                                        </Style>
                                            </StackPanel.Resources>
                                            <TextBlock Text="{DynamicResource LOCGameDetails}" Margin="0,0,0,0"
                                                       Style="{DynamicResource BaseTextBlockStyle}" FontWeight="Bold" FontSize="14"/>

                                            <Border CornerRadius="{DynamicResource ControlCornerRadius}" 
                                                    Style="{DynamicResource DetailViewCardBorder}" Margin="0,12,0,0">
                                                <GridEx x:Name="GDGrid" Margin="16,4,16,16" ColumnCount="2" StarColumns="1" 
                                                        RowCount="25" AutoLayoutColumns="2">

                                                    <Label Name="PART_ElemSeries" Content="{DynamicResource LOCSeriesLabel}" />
                                                    <ItemsControl Name="PART_ItemsSeries" />
                                                    
                                                    <Label Name="PART_ElemGenres" Content="{DynamicResource LOCGenreLabel}" />
                                                    <ItemsControl Name="PART_ItemsGenres" />

                                                    <Label Name="PART_ElemTags" Content="{DynamicResource LOCTagsLabel}" />
                                                    <ItemsControl Name="PART_ItemsTags" />

                                                    <Label Name="PART_ElemDevelopers" Content="{DynamicResource LOCDeveloperLabel}" />
                                                    <ItemsControl Name="PART_ItemsDevelopers" />

                                                    <Label Name="PART_ElemPublishers" Content="{DynamicResource LOCPublisherLabel}" />
                                                    <ItemsControl Name="PART_ItemsPublishers" />

                                                    <Label Name="PART_ElemAgeRating" Content="{DynamicResource LOCAgeRatingLabel}" />
                                                    <ItemsControl Name="PART_ItemsAgeRatings" />

                                                    <Label Name="PART_ElemRegion" Content="{DynamicResource LOCRegionLabel}" />
                                                    <ItemsControl Name="PART_ItemsRegions" />

                                                    <Label Name="PART_ElemVersion" Content="{DynamicResource LOCVersionLabel}" />
                                                    <Button Name="PART_ButtonVersion" Margin="0,12,0,0"/>

                                                    <Label Name="PART_ElemAdded" Content="{DynamicResource LOCDateAddedLabel}" />
                                                    <TextBlock Name="PART_TextAdded"  Margin="0,12,0,0" />

                                                    <Label Name="PART_ElemRecentActivity" Content="{DynamicResource LOCRecentActivityLabel}" />
                                                    <TextBlock Name="PART_TextRecentActivity" Margin="0,12,0,0"/>

                                                    <Label Name="PART_ElemSource" Content="{DynamicResource LOCSourceLabel}" />
                                                    <Button Name="PART_ButtonSource"  Margin="0,12,0,0"/>

                                                    <Label Name="PART_ElemCategories" Content="{DynamicResource LOCGameCategoriesTitle}" />
                                                    <ItemsControl Name="PART_ItemsCategories" />

                                                    <Label Name="PART_ElemLibrary" Content="{DynamicResource LOCGameProviderTitle}" />
                                                    <Button Name="PART_ButtonLibrary" Margin="0,12,0,0"/>

                                                    <StackPanel Name="PART_ElemInstallDirectory" Grid.ColumnSpan="2">
                                                        <Separator Background="{StaticResource PanelSeparatorBrush}" Height="1" Margin="0,16,0,4"/>
                                                        <Label  Content="{DynamicResource LOCGameInstallDirTitle}" />
                                                        <Button Name="PART_ButtonInstallDirectory" VerticalAlignment="Center" />
                                                        <TextBlock Name="PART_TextInstallSize" Foreground="{StaticResource PromptBrush}" FontSize="{StaticResource FontSizeSmall}" />
                                                    </StackPanel>
                                                   
       
                                                    <!--<Label Name="PART_ElemFeatures" Content="{DynamicResource LOCFeaturesLabel}" />
                                                    <ItemsControl Name="PART_ItemsFeatures" />-->

                                                </GridEx>
                                            </Border>

                                        </StackPanel>
                                        <!--Details Panel Ends-->

                                        <!--Ratings-->
                                        <StackPanel Margin="0,0,0,32">

                                            <Border CornerRadius="{DynamicResource ControlCornerRadius}" 
                                                    Style="{DynamicResource DetailViewCardBorder}" Margin="0,0,0,0">

                                                <Grid Margin="16,12,16,12">
                                                    <Grid.Resources>
                                                        <Style TargetType="Label">
                                                            <Setter Property="FontSize" Value="{StaticResource FontSizeSmaller}"/>
                                                            <Setter Property="Foreground" Value="{StaticResource IdleBrush}"/>
                                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                                        </Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="FontSize" Value="{StaticResource FontSizeLarge20}"/>
                                                            <Setter Property="Foreground" Value="{StaticResource IdleBrush}"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                                        </Style>
                                                    </Grid.Resources>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0">
                                                        <!--<Label Name="PART_ElemCommunityScore" Content="{DynamicResource LOCCommunityScore}"/>-->
                                                        <Label Content="{DynamicResource LOCCommunityScore}"/>
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                                                            <TextBlock Text="&#xe00a;" Style="{StaticResource IconFontStyle}" Margin="0,0,4,0"/>
                                                            <TextBlock Text="{Binding Game.CommunityScore, TargetNullValue='- -', FallbackValue='- -'}"
                               VerticalAlignment="Center" Margin="4,0,0,0"/>
                                                            <!--<TextBlock Name="PART_TextCommunityScore" VerticalAlignment="Center" />-->
                                                        </StackPanel>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1">
                                                        <!--<Label Name="PART_ElemCriticScore" Content="{DynamicResource LOCCriticScore}" />-->
                                                        <Label Content="{DynamicResource LOCCriticScore}" />
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="&#xe00a;" Style="{StaticResource IconFontStyle}" Margin="0,0,4,0"/>
                                                            <TextBlock Text="{Binding Game.CriticScore, TargetNullValue='- -', FallbackValue='- -'}"
                               VerticalAlignment="Center" Margin="4,0,0,0"/>
                                                            <!--<TextBlock Name="PART_TextCriticScore" VerticalAlignment="Center" />-->
                                                        </StackPanel>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="2">
                                                        <!--<Label Name="PART_ElemUserScore" Content="{DynamicResource LOCUserScore}" />-->
                                                        <Label Content="{DynamicResource LOCUserScore}" />
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="&#xe00a;" Style="{StaticResource IconFontStyle}" Margin="0,0,4,0"/>
                                                            <TextBlock Text="{Binding Game.UserScore, TargetNullValue='- -', FallbackValue='- -'}" 
                               VerticalAlignment="Center" Margin="4,0,0,0"/>
                                                            <!--<TextBlock Name="PART_TextUserScore" VerticalAlignment="Center" />-->
                                                        </StackPanel>
                                                    </StackPanel>

                                                </Grid>
                                            </Border>
                                        </StackPanel>
                                        <!--Ratings Ends-->
                                        
                                        <!--Links-->
                                        <StackPanel Name="PART_ElemLinks" Margin="0,0,0,32">
                                            <TextBlock Text="{DynamicResource LOCLinksLabel}" Style="{DynamicResource BaseTextBlockStyle}" 
                                                       FontWeight="Bold" FontSize="14" />
                                            <Border CornerRadius="{DynamicResource ControlCornerRadius}" 
                                                    Style="{DynamicResource DetailViewCardBorder}" Margin="0,12,0,0">
                                                <ItemsControl ItemsSource="{Binding Game.Links}" Margin="10,12,10,8"
                                                              Visibility="{Binding ElementName=PART_ElemLinks, Path=Visibility}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="{x:Type Link}">
                                                            <Button Content="{Binding Name}"
                                                                    Style="{StaticResource PropertyItemLinkButton}"
                                                                    ToolTip="{Binding Url}"
                                                                    Command="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.OpenLinkCommand}"
                                                                    CommandParameter="{Binding}"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Border>
                                        </StackPanel>
                                        <!--Links End-->                    

                                    </StackPanel>
                                    <!--Right Side Ends-->

                                    <!--Left Side-->
                                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,40,0">

                                        <!--Hero-->
                                        <Border Margin="0,0,0,60" Height="500" Grid.Column="0" Background="{DynamicResource ControlIdleBackgroundBrush}">
                                            <Border.OpacityMask>
                                                <VisualBrush>
                                                    <VisualBrush.Visual>
                                                        <Border Background="White" CornerRadius="{DynamicResource ControlCornerRadiusLarge}"
                                                                Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                                                                Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Border.OpacityMask>

                                            <Grid>
                                                <FadeImage x:Name="PART_ImageBackground" Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                                                <!--Video Part-->
                                                <Border>
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=EnableVideoPlayer, FallbackValue=False}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsAnyVideoAvailable, FallbackValue=False}" Value="True">
                                                                    <Setter Property="Control.Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=EnableAlternativeDetailsVideoPlayer, FallbackValue=False}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Viewbox Stretch="UniformToFill">
                                                        <ContentControl x:Name="ExtraMetadataLoader_VideoLoaderControl" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                    </Viewbox>
                                                </Border>
                                                <!--Video Part Ends-->

                                                <Border Background="{DynamicResource LightMaskBrush}"/>

                                                <DockPanel VerticalAlignment="Bottom" Margin="40,40,40,0" LastChildFill="False">

                                                    <!--ImageCover-->
                                                    <Border HorizontalAlignment="Center" DockPanel.Dock="Left">
                                                        <Image Name="PART_ImageCover" VerticalAlignment="Bottom" Margin="0,0,0,-40"
                                                               HorizontalAlignment="Center" Width="160"
                                                               StretchDirection="Both" Stretch="Uniform"
                                                               RenderOptions.BitmapScalingMode="Fant" >
                                                            <Image.OpacityMask>
                                                                <VisualBrush>
                                                                    <VisualBrush.Visual>
                                                                        <Border Background="Black" CornerRadius="8"
                                                                                Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Image}}"
                                                                                Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Image}}" />
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Image.OpacityMask>
                                                        </Image>
                                                    </Border>
                                                    <!--ImageCover Ends-->

                                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="24,0,0,-28"
                                                                VerticalAlignment="Bottom" HorizontalAlignment="Stretch">

                                                        <Button Name="PART_ButtonReleaseDate" Margin="0,5,8,5" Style="{DynamicResource PropertyItemSmallButton}"/>

                                                        <StackPanel Name="PART_ElemPlatform" Margin="0,3,0,3" VerticalAlignment="Top" HorizontalAlignment="Left" >
                                                            <StackPanel.Style>
                                                                <Style TargetType="StackPanel">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Settings DetailsVisibility.Platforms}" Value="False">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Game.Platforms}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </StackPanel.Style>

                                                            <ItemsControl ItemsSource="{Binding Game.Platforms}" Margin="0,0,0,0">
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <WrapPanel Orientation="Horizontal"/>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Button Content="{Binding Name}" Style="{DynamicResource PropertyItemSmallButton}" Margin="0,2,8,2" 
                                                                                Command="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=DataContext.SetFeatureFilterCommand}" CommandParameter="{Binding}"/>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </StackPanel>

                                                        <StackPanel Orientation="Horizontal" Margin="16,0,24,0">
                                                            <StackPanel.Resources>
                                                                <SolidColorBrush x:Key="Extras_FilledStarBrush" Color="{StaticResource TextColor}"/>
                                                                <SolidColorBrush x:Key="Extras_EmptyStarBrush" Color="{StaticResource TextColor}" Opacity="0.2"/>
                                                            </StackPanel.Resources>

                                                            <ContentControl x:Name="ThemeExtras_CriticRating" Margin="0,4,0,0"/>
                                                            <TextBlock Text="{Binding Game.CriticScore, TargetNullValue='- -', FallbackValue='- -'}" 
                                                                       Margin="4,0,0,0" VerticalAlignment="Center"
                                                                       FontSize="{DynamicResource FontSize}" Style="{DynamicResource BaseTextBlockStyle}"/>
                                                        </StackPanel>

                                                        <!--<WrapPanel Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Stretch">
                                                            <ItemsControl ItemsSource="{Binding Game.Features}" Margin="0,0,0,0" >
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Button Content="{Binding Name}" Margin="0,2,8,2"
                                                                                Style="{DynamicResource PropertyItemTagButton}"
                                                                                Command="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=DataContext.SetFeatureFilterCommand}" CommandParameter="{Binding}"/>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>

                                                            <ItemsControl ItemsSource="{Binding Game.Genres}" Margin="0,0,0,0" >
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Button Content="{Binding Name}" Margin="0,2,8,2"
                                                                                Style="{DynamicResource PropertyItemTagButton}"
                                                                                Command="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=DataContext.SetFeatureFilterCommand}" CommandParameter="{Binding}"/>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>

                                                        </WrapPanel>-->

                                                    </StackPanel>

                                                    <TextBlock Name="PART_TextDisplayName" FontFamily="{DynamicResource FontFamily}"
                                                               FontSize="{DynamicResource FontSizeLarge28}" FontWeight="Bold" Margin="24,0,0,0" DockPanel.Dock="Bottom"
                                                               TextWrapping="Wrap" Foreground="{DynamicResource SelectedBrush}"/>

                                                </DockPanel>
                                            </Grid>
                                        </Border>
                                        <!--Hero Ends-->

                                        <StackPanel Margin="0,0,0,40">
                                            <Border CornerRadius="{DynamicResource ControlCornerRadius}" 
                                                    Style="{DynamicResource DetailViewCardBorder}" Margin="0,12,0,0">
                                                <Grid Margin="16,12,16,14">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.Resources>
                                                        <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
                                                            <Setter Property="Margin" Value="0,4,0,2" />
                                                            <Setter Property="VerticalAlignment" Value="Top" />
                                                            <Setter Property="Foreground" Value="{DynamicResource IdleBrush}" />
                                                            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
                                                        </Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                                            <Setter Property="RenderOptions.BitmapScalingMode" Value="Fant"/>
                                                            <Setter Property="Height" Value="40"/>
                                                            <Setter Property="Margin" Value="0,0,10,0"/>
                                                        </Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </Style>
                                                    </Grid.Resources>
                                                    
                                                    <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemLastPlayed, Mode=OneWay}" Grid.Column="0">
                                                        <Image Source="{ThemeFile 'Images/3DIcons/calendar.png'}"  DockPanel.Dock="Left"/>
                                                        <Label Name="PART_ElemLastPlayed" Content="{DynamicResource LOCLastPlayed}" DockPanel.Dock="Top"/>
                                                        <TextBlock Name="PART_TextLastActivity" DockPanel.Dock="Top"/>
                                                    </DockPanel>

                                                    <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemPlayTime, Mode=OneWay}" Grid.Column="1">
                                                        <Image Source="{ThemeFile 'Images/3DIcons/clock.png'}"  DockPanel.Dock="Left"/>
                                                        <Label Name="PART_ElemPlayTime" Content="{DynamicResource LOCTimePlayed}" DockPanel.Dock="Top"/>
                                                        <TextBlock Name="PART_TextPlayTime" DockPanel.Dock="Top"/>
                                                    </DockPanel>

                                                    <DockPanel Visibility="{Binding Visibility, ElementName=PART_ElemCompletionStatus, Mode=OneWay}" Grid.Column="2">
                                                        <Image Source="{ThemeFile 'Images/3DIcons/check.png'}"  DockPanel.Dock="Left"/>
                                                        <Label Name="PART_ElemCompletionStatus" Content="{DynamicResource LOCCompletionStatus}" DockPanel.Dock="Top"/>
                                                        <Button Name="PART_ButtonCompletionStatus" DockPanel.Dock="Top" FontWeight="Bold" Margin="0,1,0,0"
                                                                Style="{DynamicResource PropertyItemSmallButton}" FontSize="14"/>
                                                    </DockPanel>

                                                    <DockPanel Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Column="3" >
                                                        <Image Source="{ThemeFile 'Images/3DIcons/trophy.png'}"  DockPanel.Dock="Left"/>
                                                        <StackPanel Name="PART_SuccessStory" DockPanel.Dock="Top">
                                                            
                                                            <Label Content="{DynamicResource LOCSuccessStoryAchievements}"/>
                                                            
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                                <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Unlocked, FallbackValue='-'}"/>
                                                                <TextBlock Text="/"/>
                                                                <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Total, FallbackValue='-'}"/>
                                                                <ProgressBar x:Name="progressBar" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"
                                                                             Maximum="{PluginSettings Plugin=SuccessStory, Path=Total, FallbackValue=0}" Value="{PluginSettings Plugin=SuccessStory, Path=Unlocked, FallbackValue=0}" HorizontalAlignment="Left"
                                                                             Background="{DynamicResource ControlHoverBackgroundBrush }" Foreground="{DynamicResource GlyphBrush}" Width="60" Height="10" Style="{DynamicResource RoundedProgressBar}">
                                                                    <ProgressBar.Resources>
                                                                        <Style x:Key="RoundedProgressBar" TargetType="ProgressBar">
                                                                            <Setter Property="Background" Value="{DynamicResource NormalBrushDark}"/>
                                                                            <Setter Property="BorderThickness" Value="{DynamicResource ControlBorderThickness}"/>
                                                                            <Setter Property="Template">
                                                                                <Setter.Value>
                                                                                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                                                                                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                                                                                                CornerRadius="{DynamicResource ControlCornerRadius}">
                                                                                            <Grid SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                                                                                <Rectangle Fill="{TemplateBinding Background}" RadiusY="4" RadiusX="4"/>
                                                                                                <Rectangle x:Name="PART_Track" Margin="0" RadiusY="4" RadiusX="4"/>
                                                                                                <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left" VerticalAlignment="Stretch">
                                                                                                    <Grid>
                                                                                                        <Rectangle Fill="{TemplateBinding Foreground}" RadiusY="4" RadiusX="4"/>
                                                                                                    </Grid>
                                                                                                </Decorator>
                                                                                            </Grid>
                                                                                        </Border>
                                                                                    </ControlTemplate>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Style>
                                                                    </ProgressBar.Resources>
                                                                </ProgressBar>
                                                            </StackPanel>

                                                        </StackPanel>
                                                    </DockPanel>

                                                </Grid>
                                                
                                            </Border>
                                        </StackPanel>

                                        <!--Tab Table-->
                                        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Stretch">

                                            <TabControl Margin="0,0,0,16" VerticalAlignment="Top" HorizontalAlignment="Stretch"
                                                        Style="{StaticResource DetailViewTabControl}">
                                                <!--Tab Overview-->
                                                <TabItem Header="{DynamicResource LOCOverviewLabel}" x:Name="DescriptionTab" FontSize="16" >

                                                    <StackPanel Margin="0,24,0,0">

                                                        <StackPanel x:Name="DescriptionContent" Margin="0,0,0,0" >
                                                            <StackPanel Name="PART_ElemDescription">
                                                                <HtmlTextView Name="PART_HtmlDescription" Margin="0,0,0,0" HorizontalAlignment="Stretch"
                                                                              HtmlFontSize="{DynamicResource FontSize}"
                                                                              HtmlFontFamily="{DynamicResource FontFamily}"
                                                                              HtmlForeground="{DynamicResource TextColor}"
                                                                              LinkForeground="{DynamicResource AccentSelectedColor}" 
                                                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                                              ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                                                            </StackPanel>
                                                        </StackPanel>

                                                        <StackPanel Margin="0,24,0,0" Visibility="{Binding ElementName=GameRelationsVisibilityProxy, Path=Tag}">

                                                            <TextBlock Text="{DynamicResource LOCGameRelations_RelatedGamesLabel}" Margin="0,0,0,16"
                                                                       FontSize="{DynamicResource FontSizeLarge20}" FontWeight="Bold" Foreground="{DynamicResource SelectedBrush}"/>

                                                            <TextBlock x:Name="GameRelationsVisibilityProxy" Visibility="Collapsed">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Tag" Value="Collapsed" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding ElementName=Panel_GameRelations_SimilarGamesControl, Path=Visibility}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding ElementName=Panel_GameRelations_SameSeriesControl, Path=Visibility}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding ElementName=Panel_GameRelations_SameDeveloperControl, Path=Visibility}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding ElementName=Panel_GameRelations_SamePublisherControl, Path=Visibility}" Value="Visible">
                                                                                <Setter Property="Tag" Value="Visible" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                            <Expander x:Name="Panel_GameRelations_SimilarGamesControl"
                                  Header="{DynamicResource LOCGameRelations_SimilarGamesLabel}"
                                  IsExpanded="True" Margin="0,8,0,0"
                                  Visibility="{PluginSettings Plugin=GameRelations, Path=SimilarGamesControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                                <ContentControl x:Name="GameRelations_SimilarGamesControl" Margin="0,16,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SameSeriesControl"
                                  Header="{DynamicResource LOCGameRelations_SameSeriesLabel}"
                                  IsExpanded="True" Margin="0,8,0,0"
                                  Visibility="{PluginSettings Plugin=GameRelations, Path=SameSeriesControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                                <ContentControl x:Name="GameRelations_SameSeriesControl" Margin="0,16,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SameDeveloperControl"
                                  Header="{DynamicResource LOCGameRelations_SameDeveloperLabel}"
                                  IsExpanded="True" Margin="0,8,0,0"
                                  Visibility="{PluginSettings Plugin=GameRelations, Path=SameDeveloperControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                                <ContentControl x:Name="GameRelations_SameDeveloperControl" Margin="0,16,0,0" />
                                                            </Expander>

                                                            <Expander x:Name="Panel_GameRelations_SamePublisherControl"
                                  Header="{DynamicResource LOCGameRelations_SamePublisherLabel}"
                                  IsExpanded="True" Margin="0,8,0,0"
                                  Visibility="{PluginSettings Plugin=GameRelations, Path=SamePublisherControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                                <ContentControl x:Name="GameRelations_SamePublisherControl" Margin="0,16,0,0" />
                                                            </Expander>
                                                        </StackPanel>

                                                    </StackPanel>
                                                </TabItem>
                                                <!--Tab Overview Ends-->

                                                <!--Tab Notes-->
                                                <TabItem Header="{DynamicResource LOCNotesLabel}" FontSize="16"
                                                         Visibility="{Binding ElementName=NotesVisibilityProxy, Path=Tag}">
                                                    <StackPanel Margin="0,24,0,0">
                                                        <TextBlock x:Name="NotesVisibilityProxy" Visibility="Collapsed">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Tag" Value="Collapsed" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding ElementName=PART_TextNotes, Path=Visibility}" Value="Visible">
                                                                            <Setter Property="Tag" Value="Visible" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>

                                                        <TextBox Name="PART_TextNotes" IsReadOnly="True" BorderThickness="0"
                                                                 AcceptsReturn="True" TextWrapping="Wrap"
                                                                 Margin="-1,5,-1,5" Padding="0" Background="Transparent" FontWeight="Normal"/>
                                                    </StackPanel>
                                                </TabItem>
                                                <!--Tab Notes Ends-->

                                                <TabItem Header="{DynamicResource LOCSuccessStoryAchievements}"
                         Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Margin="0,24,0,0">
                                                        <ContentControl x:Name="SuccessStory_PluginCompactUnlocked" Margin="0,0,0,24" />
                                                        <ContentControl x:Name="SuccessStory_PluginCompactLocked" Margin="0,0,0,24" />
                                                        <ContentControl x:Name="SuccessStory_PluginUserStats" Margin="0,0,0,24" />
                                                        <ContentControl x:Name="SuccessStory_PluginList" Margin="0,0,0,24" MinHeight="300" />
                                                        <ContentControl x:Name="SuccessStory_PluginChart" Margin="0,0,0,24" />
                                                    </StackPanel>
                                                </TabItem>

                                                <TabItem Header="{DynamicResource LOCSsvTitle}"
                         Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Orientation="Vertical" Background="{TemplateBinding Background}" Margin="0,24,0,0">
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginListScreenshotsVertical" Margin="0,0,0,0" Height="500"/>
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginSinglePicture" />
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginListScreenshots" />
                                                    </StackPanel>

                                                </TabItem>

                                            </TabControl>
                                        </StackPanel>
                                        <!--Tab Table Ends-->

                                        <!--Right Part-->
                                        <StackPanel Margin="0,0,0,40" Grid.Row="1" Grid.Column="1">

                                            <!-- SuccessStory -->
                                            <StackPanel Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, Mode=OneWay, FallbackValue={x:Static Visibility.Collapsed}}">

                                                <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,0,0,24"/>

                                                <DockPanel Margin="0,0,0,24">
                                                    <ContentControl x:Name="SuccessStory_PluginCompactList" DockPanel.Dock="Bottom" Margin="0,16,0,0" />
                                                    <ContentControl x:Name="SuccessStory_PluginProgressBar" DockPanel.Dock="Bottom" Margin="0,16,0,0" VerticalAlignment="Center" Height="24"/>
                                                    <!--<ContentControl x:Name="SuccessStory_PluginButton" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center"/>-->
                                                    <TextBlock Text="{DynamicResource LOCSuccessStoryAchievements}" DockPanel.Dock="Left" VerticalAlignment="Center"
                       Style="{DynamicResource BaseTextBlockStyle}" FontWeight="Bold" FontSize="20"/>
                                                </DockPanel>
                                            </StackPanel>



                                        </StackPanel>
                                        <!--Right Part Ends-->

                                    </StackPanel>
                                    <!--Left Side Ends-->
                                    
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                        
                        <!--Bottom Fixed Part-->
                        <!--<Grid MaxWidth="1280" MinWidth="1024"  Grid.Row="1" >
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Style.Triggers>
                                        <Trigger Property="DataContext" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Border Height="56" HorizontalAlignment="Center" Margin="16" Padding="4,8,4,8"
                                    Background="{DynamicResource PopupBackgroundBrush}"
                                    BorderBrush="{DynamicResource PopupBorderBrush}" 
                                    CornerRadius="{DynamicResource ControlCornerRadiusLarge}"
                                    BorderThickness="{DynamicResource PopupBorderThickness}">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="4" Opacity="0.16" Direction="270"/>
                                </Border.Effect>

                                
                            </Border>


                        </Grid>-->
                        <!--Bottom Fixed Part Ends-->
                    </Grid>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>